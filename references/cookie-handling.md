# Cookie handling

Use only cookies from an account with lawful access to the target post.

Recommended input: Netscape `cookies.txt` exported locally from the user's browser. Do not paste cookie values into chat. Pass the file path with `--cookies`.

The tool reads cookies for fetches but does not copy them into the export folder.
